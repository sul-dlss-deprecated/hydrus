<%- @fobj.contributors.each_with_index do |person, index| -%>
  <%= render :partial => "hydrus_items/person", :locals=> {:person=> person, :index=>index} %>
<%- end -%>

<%= button_tag("Add Person", :name=>"add_person", :"data-attribute" => "add_person", :id=>"add_person", :class=>"btn", :value=>@fobj.contributors.length) %>


<%# f.select(
      "license_#{lic_opt}".to_sym,
      grouped_options_for_select(
        Hydrus::GenericObject.license_groups,
        @fobj.send("license_#{lic_opt}")
      ),
      {:prompt => 'Select license'},
      {:class => 'inline-select right', :id => "license_option_#{lic_opt}" }
    )
%>
